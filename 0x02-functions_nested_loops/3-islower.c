#include "main.h"
/**
 * print_alphabet_x10 - a function that prints the alphabet 10 times, in lowercase
 *
 * Return: Always 0 (Success)
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
		return (1);
	else
		return (0);
}
