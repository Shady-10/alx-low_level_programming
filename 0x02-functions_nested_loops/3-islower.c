#include "main.h"
/**
 * print_alphabet_x10 - a function that prints the alphabet 10 times, in lowercase
 *
 * Return: Always 0 (Success)
 */
int _islower(int c)
{
	if (c < 97 and c > 122)
		return 0;
	else
		return 1
}
